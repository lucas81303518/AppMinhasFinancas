unit funcoes;

interface

uses
  FMX.ListBox;

function GetSelectedObject(combobox: TCombobox): Object;

implementation

function GetSelectedObject(combobox: TCombobox): Object;
begin
  if combobox.ItemIndex <> -1 then
    Result := combobox.Items.Objects[combobox.ItemIndex]
  else
    Result := nil; // Retorna nil se nenhum item estiver selecionado
end;


end.
